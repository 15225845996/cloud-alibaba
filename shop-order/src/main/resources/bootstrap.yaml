spring:
  application:
    name: service-order
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
  profiles:
    active: dev

  alibaba:
    seata:
      tx-service-group: ${spring.application.name}


server:
  port: 8093